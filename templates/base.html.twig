<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>
            {% block title %}
                Welcome!
                			
            {% endblock %}
        </title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
        <link rel="stylesheet" href="path/to/font-awesome/css/font-awesome.min.css">
          {% block javascript %}
          {% endblock %}
    </head>
    <style>
        .container {
            padding-top: 20px;
            font-family: 'Times New Roman', Times, serif;
        }
        
        h5{
            text-transform: uppercase;
            font-size: 13;
        }
        .span{
            font-size: 13px;
            color: black;
            font-style: normal;
            font-weight: bolder;
        }
        .show_produit{
            padding-top: 25px;
        }

        .container-block{
          max-width: 50%;
          margin: 0 auto;
        }

        .form-check{
          background-color: #f4f8ff;
          padding: 20px 65px;
          margin-top: 8px;

        }

        .col-form-label {
          font-weight: bold;
        }
</style>
    </style>
    <body>
        <header>
        <!-- Navbar -->
        <nav class="navbar navbar-expand-lg navbar-light bg-white fixed-top">
          <div class="container-fluid">
            <button
              class="navbar-toggler"
              type="button"
              data-mdb-toggle="collapse"
              data-mdb-target="#navbarExample01"
              aria-controls="navbarExample01"
              aria-expanded="false"
              aria-label="Toggle navigation"
            >
              <i class="fas fa-bars"></i>
            </button>
            <div class="collapse navbar-collapse" id="navBarCollapse">
              <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item active">
                  <a class="nav-link" aria-current="page" href="#">Acceuil</a>
                </li>
                <li class="nav-item">
                  <a class="nav-link" href="{{path('app_product')}}">Nos produits</a>
                </li>
                </ul>
                <div class="navbar-item-custom">
                      <a href="{{path('app_cart')}}"><img src="{{asset('icon/cart.jpg')}}" alt="cart" height="30px"></a>   
                    {% if app.user %}
                        <a href="{{path('app_compte')}}">Mon Compte : <small>{{ app.user.nom }}</small></a> | <a href="{{path('app_logout')}}">Deconnexion</a>      
                    {% else %}
                        <a href="{{path('app_login')}}">Connexion</a>   | <a href="{{path('app_register')}}">Inscription</a>
                    {% endif %}
                  </ul> 
              </div>
            </div>
          </div>
        </nav>
        <!-- Navbar -->
      
        <!-- Background image -->
        <div
          class="p-5 text-center bg-image"
          style="
            background-image: url('https://mdbcdn.b-cdn.net/img/new/slides/041.webp'); height: 400px; margin-top: 58px;">
        </div>
        <!-- Background image -->
      </header>
      <div class="container">
        {% block body %}{% endblock %}
      </div>
    </body>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
    {% block script %}
    {% endblock %}
</html>
